services:
  portal:
    image: nginx:alpine
    container_name: mhcloud-portal
    volumes:
      - ../../frontend:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    ports:
      - "3000:80"
    restart: unless-stopped
